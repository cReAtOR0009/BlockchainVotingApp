{
  "name": "blockchain_voting_platform",
  "version": "1.0.0",
  "description": "a blockchain app that allows you to create election, vote and get all election details for elections created on the eth Blockchain",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "solcjs --bin --abi --include-path node_modules/ --base-path . -o . Vote.sol"
  },
  "keywords": [
    "blockchain",
    "eth"
  ],
  "author": "_creator",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.9.2",
    "fs-extra": "^11.2.0",
    "solc": "^0.8.7-fixed"
  }
}
